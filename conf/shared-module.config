// modules.config - dedicated file for shared module configurations
// TODO go through and fix
process {
    withName: 'EXAMPLE_MODULE' {
        ext.args = { meta.option ? '--option-flag1' : '--option-flag2' }
        ext.args2 = '--output-stats'
        ext.when = { !params.skip_example }
    }
}